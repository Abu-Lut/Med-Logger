<!DOCTYPE html>
<html lang="en">
	<head>
    	<meta charset="utf-8">
    	<meta name="description" content="This is where your description goes">
    	<meta name="keywords" content="one, two, three">
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Poppins&display=swap" rel="stylesheet">
		<title>Title in the tab</title>

		<!-- external CSS link -->
		<link rel="stylesheet" href="/css/normalize.css">
		<link rel="stylesheet" href="/css/style.css">
	</head>
	<body>
		<h1>MedLogs</h1>

		<section class="one">
			<h2>Medicines to take now</h2>

			<ul class="meds">
				<% for(let i=0; i < info.length; i++) {%>
					<li class="med">
						<% if (info[i].medStatus=="true"){ %>
							<span class="done"><%= info[i].medName %></span>
							<span class="done"><%= info[i].medTiming %></span>
							<span class="done"><%= info[i].medStatus %></span>
							<span class="fa-solid fa-check check done"></span>
							<span class="fa-regular fa-x x"></span>
							
						<% }else {%>
							<span><%= info[i].medName %></span>
							<span><%= info[i].medTiming %></span>
							<span><%= info[i].medStatus %></span>
							<span class="fa-solid fa-check check" ></span>
							<span class="fa-regular fa-x x"></span>
						<% } %>
					</li>
				<% } %>
				</ul>

		</section>


	
		<section class="two">
			<h2>Modify Medicine Schedule</h2>
			<form action="/addRapper" method="POST">
				<input type="text" placeholder="Medicine Name" name="medName">
				<input type="text" placeholder="Timing" name="timing">
				<input type="submit">
			</form>
		</section>
	
		<section class="three">
			<h2>Medicine Logs</h2>
		</section>

		<script type="text/javascript" src="js/main.js"></script>
	</body>
</html>
